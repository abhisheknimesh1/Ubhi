{
  "name": "ubhi-site-build",
  "private": true,
  "version": "1.0.0",
  "description": "Build step for the Ubhi static site: minifies JS + CSS into a production-ready dist/ folder. Source files are never modified.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.31.6"
  }
}
